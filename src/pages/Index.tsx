
import { Redirect } from 'react-router-dom';
import Dashboard from './Dashboard';

// This page now simply redirects to the Dashboard component
const Index = () => {
  return <Dashboard />;
};

export default Index;
